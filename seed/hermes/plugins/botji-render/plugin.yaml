name: botji-render
version: 1.0.0
description: "V1R render plugin: providers/openai_codex + operations (exact_copy, render_schema, edit_image). Owns provider routing and image generation during the botji-artifacts cutover."
author: Botji
kind: standalone
requires_env: []
provides_hooks: []
