Render the source image using the exact brief below. The brief is the complete specification — follow every constraint precisely.

---

{{INSTRUCTIONS}}

---

Before rendering, verify internally:
- Is the left-to-right module order exactly as specified in the SUBJECT list?
- Are all FORBIDDEN elements absent from the output?
- Are all HARD PRESERVE constraints satisfied?
- Are all directly-adjacent modules touching with no gap or filler between them?

If any constraint would be violated to achieve a more aesthetically "natural" result, choose the constraint. Fidelity to the brief outweighs photographic convention.
