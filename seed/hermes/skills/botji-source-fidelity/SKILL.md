---
name: botji-source-fidelity
description: DEPRECATED — use botji-artifact-fidelity for the artifact pipeline and botji-fidelity-rules for the blocking rules table.
tags: [botji, deprecated]
---

# Deprecated

This skill was merged into **botji-artifact-fidelity** (pipeline + design-proposal mode + sketch-to-render thresholds) and **botji-fidelity-rules** (blocking rules table + image generation rules).

**Stop reading this skill.** If you arrived here looking for the 8-axis review schema, blocking conditions, or the artifact loop, use:

- `botji-artifact-fidelity` — the full register → transform → review pipeline
- `botji-fidelity-rules` — concise rules card
